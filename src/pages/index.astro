---
import { getArticles } from '../lib/articles';
import ArticleIndex from '../components/ArticleIndex.astro';

const articles = await getArticles();
---

<ArticleIndex articles={articles} page={1} />
