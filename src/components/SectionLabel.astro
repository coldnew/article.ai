---
const { eyebrow, title, href, linkText = '查看全部' } = Astro.props;
---
<div class="section-heading"><div><span class="eyebrow">{eyebrow}</span><h2>{title}</h2></div>{href && <a class="section-link" href={href}>{linkText} <span>↗</span></a>}</div>
